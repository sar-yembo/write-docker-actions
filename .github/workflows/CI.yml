name: CI

on: [push]

jobs:
  action:
    runs-on: ubuntu-latest

    steps:
      - name: checkout repo
        uses: actions/checkout@v1

      - name: build and config PHP Docker
        uses: ./.github/actions/run-phpunit